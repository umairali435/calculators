import 'package:flutter/material.dart' show Color;
import 'package:flutter/foundation.dart' show immutable;

@immutable
class AppColors {
  const AppColors._();
  static const Color primaryColor = Color(0xFF219C90);
  static const Color whiteColor = Color(0xFFFFFFFF);
}
